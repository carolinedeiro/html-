const funcao = function (){
            let quantidade = document.getElementById (" insira a quantidade");
            if( quantidade.value.length == 0)
            alert("quantidade, " + acc);
            else
                  
                    alert (" Valor total, " + quantidade.Value);
            
            

        };

        let botao = document.querySelector ('# botao');
        botao.addEventListener ( "clik", (nome, funcao);

             var acc = 0;

            var arrayquantidade = Document.querySelectorall (".quantidade");

            for ( var i = 0; i< arreyquantidade.length; i++){
                    arrayquantidade[i].Value;
                    acc = acc + arrayquantidade

            }
          
                
            }; 